# Railway / Railpack — install the API from this monorepo.
# Do not use -e path[extras]; pip treats that as an invalid editable URL.
./packages/ingestion
starlette>=0.37
uvicorn>=0.27
sentence-transformers>=3.0
